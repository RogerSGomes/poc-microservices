const { CreateProfessorDTO } = require('./create-professor.dto');
const { SignUpDTO } = require('./sign-up.dto');
const { SignInDTO } = require('./sign-in.dto');

module.exports = { CreateProfessorDTO, SignUpDTO, SignInDTO };
